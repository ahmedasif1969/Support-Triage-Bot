# Root-level settings, shared across all clients.
# This is YOUR ops channel — where you get paged if a client's run fails,
# separate from that client's own SLACK_WEBHOOK_URL (set per-client instead,
# in clients/<name>/.env).
OPS_SLACK_WEBHOOK_URL=
